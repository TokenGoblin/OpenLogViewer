<Application x:Class="OpenLogViewer.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Used for both the list rows and the closed state of the axis pickers. -->
        <DataTemplate x:Key="ChannelName">
            <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"/>
        </DataTemplate>

        <DataTemplate x:Key="AxisSourceName">
            <TextBlock Text="{Binding Label}" TextTrimming="CharacterEllipsis"/>
        </DataTemplate>

        <DataTemplate x:Key="ThemeName">
            <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"/>
        </DataTemplate>

        <!-- Seed values only. ThemeManager overwrites every one of these at
             startup and on each theme change; everything below therefore binds
             with DynamicResource, or the window would keep the first theme it
             was parsed with. -->
        <SolidColorBrush x:Key="Bg"        Color="#14171C"/>
        <SolidColorBrush x:Key="Panel"     Color="#1B1F26"/>
        <SolidColorBrush x:Key="PanelAlt"  Color="#222831"/>
        <SolidColorBrush x:Key="Hover"     Color="#2E3742"/>
        <SolidColorBrush x:Key="Selected"  Color="#25333F"/>
        <SolidColorBrush x:Key="Header"    Color="#1E242C"/>
        <SolidColorBrush x:Key="Line"      Color="#2C333E"/>
        <SolidColorBrush x:Key="Text"      Color="#DDE3EA"/>
        <SolidColorBrush x:Key="Muted"     Color="#828D9E"/>
        <SolidColorBrush x:Key="Faint"     Color="#6B7686"/>
        <SolidColorBrush x:Key="Scroll"    Color="#5E6674"/>
        <SolidColorBrush x:Key="ScrollHover" Color="#828D9E"/>
        <SolidColorBrush x:Key="Accent"    Color="#4FC3F7"/>
        <SolidColorBrush x:Key="OnAccent"  Color="#12161B"/>
        <SolidColorBrush x:Key="Marker"    Color="#FFB34D"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="Background" Value="{DynamicResource PanelAlt}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="4"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource Hover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="Background" Value="{DynamicResource Bg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="CaretBrush" Value="{DynamicResource Text}"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
        </Style>

        <!-- Segmented selector used for the channel sort mode. -->
        <Style x:Key="Chip" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="b" CornerRadius="3" Margin="0,0,4,0" Padding="8,3"
                                Background="Transparent"
                                BorderBrush="{DynamicResource Line}" BorderThickness="1">
                            <TextBlock x:Name="t" FontSize="11"
                                       Text="{TemplateBinding Content}"
                                       Foreground="{DynamicResource Muted}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="t" Property="Foreground" Value="{DynamicResource Text}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource Selected}"/>
                                <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                <Setter TargetName="t" Property="Foreground" Value="{DynamicResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Jump-to-extreme buttons on a channel row. Always visible: hover-only
             controls are hard to hit, because the pointer has to stay inside the
             row on the way to a target that only appeared once it got there. -->
        <Style x:Key="JumpButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="26"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="3" Margin="1,0"
                                Background="{DynamicResource PanelAlt}"
                                BorderBrush="{DynamicResource Line}" BorderThickness="1">
                            <TextBlock x:Name="t" Text="{TemplateBinding Content}"
                                       FontSize="12" FontWeight="Bold"
                                       Foreground="{DynamicResource Muted}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource Accent}"/>
                                <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                <Setter TargetName="t" Property="Foreground" Value="{DynamicResource OnAccent}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.25"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                      RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="b" CornerRadius="3" Padding="7,4"
                                                Background="{DynamicResource Bg}"
                                                BorderBrush="{DynamicResource Line}" BorderThickness="1">
                                            <TextBlock Text="&#xE70D;" FontFamily="Segoe MDL2 Assets"
                                                       FontSize="8" Foreground="{DynamicResource Muted}"
                                                       HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="b" Property="BorderBrush"
                                                        Value="{DynamicResource Accent}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <!-- Driven from ItemTemplate rather than SelectionBoxItemTemplate:
                                 the latter does not resolve through a replaced control
                                 template, and the box then shows the type name. -->
                            <ContentPresenter IsHitTestVisible="False" Margin="8,0,22,0"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{Binding ItemTemplate,
                                                                RelativeSource={RelativeSource TemplatedParent}}"/>

                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                                   AllowsTransparency="True" Focusable="False" StaysOpen="False">
                                <Border Background="{DynamicResource PanelAlt}" CornerRadius="3"
                                        BorderBrush="{DynamicResource Line}" BorderThickness="1"
                                        MaxHeight="360"
                                        MinWidth="{Binding ActualWidth,
                                                   RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="b" Background="Transparent" Padding="9,5">
                            <ContentPresenter ContentTemplate="{TemplateBinding ContentTemplate}"
                                              ContentStringFormat="{TemplateBinding ContentStringFormat}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource Hover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource Selected}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- A saved channel selection, shown as a chip. -->
        <Style x:Key="PresetChip" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="9" Margin="0,0,4,4" Padding="9,3"
                                Background="{DynamicResource PanelAlt}"
                                BorderBrush="{DynamicResource Line}" BorderThickness="1">
                            <TextBlock x:Name="t" Text="{TemplateBinding Content}" FontSize="11"
                                       Foreground="{DynamicResource Text}"
                                       TextTrimming="CharacterEllipsis" MaxWidth="150"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource Hover}"/>
                                <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                <Setter TargetName="t" Property="Foreground" Value="{DynamicResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Menus.

             Templated rather than merely recoloured. Windows' own menu template
             paints a fixed light strip down the left for icons, and no amount of
             setting Background reaches it — on a dark scheme it shows as a white
             gutter beside every item, which is the same complaint as a white
             title bar in a smaller place. -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource PanelAlt}"/>
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="4"
                                SnapsToDevicePixels="True">
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Background" Value="{DynamicResource Line}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1" Background="{TemplateBinding Background}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Menu">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Menu">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid>
                            <Border x:Name="row" Background="{TemplateBinding Background}"
                                    CornerRadius="4" SnapsToDevicePixels="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition x:Name="tickColumn" Width="20"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock x:Name="tick" Grid.Column="0" Text="✓"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Visibility="Collapsed"
                                               Foreground="{DynamicResource Accent}"/>

                                    <ContentPresenter Grid.Column="1" ContentSource="Header"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"
                                                      RecognizesAccessKey="True"/>

                                    <TextBlock x:Name="more" Grid.Column="2" Text="›"
                                               Margin="10,0,10,0" VerticalAlignment="Center"
                                               Visibility="Collapsed"
                                               Foreground="{DynamicResource Muted}"/>
                                </Grid>
                            </Border>

                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Right" HorizontalOffset="-3" VerticalOffset="-8">
                                <Border Background="{DynamicResource PanelAlt}"
                                        BorderBrush="{DynamicResource Line}"
                                        BorderThickness="1" CornerRadius="6" Padding="4"
                                        SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="True">
                                <Setter TargetName="more" Property="Visibility" Value="Visible"/>
                            </Trigger>

                            <!--
                                A name on the menu bar is not a row in a menu.

                                After this trigger, deliberately, because it has
                                to win over the arrow that every item with
                                children otherwise gets: "File ›" reads as though
                                File were itself inside something. There is no
                                room for a tick either, and the list belongs
                                below the name rather than beside it.
                            -->
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Setter TargetName="tickColumn" Property="Width" Value="0"/>
                                <Setter TargetName="more" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                                <Setter TargetName="PART_Popup" Property="HorizontalOffset" Value="0"/>
                                <Setter TargetName="PART_Popup" Property="VerticalOffset" Value="2"/>
                            </Trigger>

                            <Trigger Property="Role" Value="TopLevelItem">
                                <Setter TargetName="tickColumn" Property="Width" Value="0"/>
                                <Setter TargetName="more" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="tick" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="row" Property="Background"
                                        Value="{DynamicResource Hover}"/>
                            </Trigger>

                            <!-- Disabled items are used as captions here, so they
                                 have to read as text rather than as something
                                 broken. -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Muted}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Scrollbars.

             Slim, with no stepper arrows: the sidebar is scrolled by wheel and
             by dragging, and a pair of 17px arrow buttons at each end would take
             more room than they earn. The track stays transparent so the bar
             reads as part of the panel it sits in rather than as a channel cut
             through it. -->
        <Style x:Key="ScrollThumb" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="b" CornerRadius="3" Margin="2"
                                Background="{DynamicResource Scroll}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background"
                                        Value="{DynamicResource ScrollHover}"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="b" Property="Background"
                                        Value="{DynamicResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- The page-up/page-down halves of the track. Invisible, but they must
             stay hit-testable or clicking beside the thumb would do nothing. -->
        <Style x:Key="ScrollPage" TargetType="RepeatButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="VerticalScrollBar" TargetType="ScrollBar">
            <Grid Background="Transparent">
                <Track x:Name="PART_Track" IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollPage}"
                                      Command="ScrollBar.PageUpCommand"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollThumb}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollPage}"
                                      Command="ScrollBar.PageDownCommand"/>
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <ControlTemplate x:Key="HorizontalScrollBar" TargetType="ScrollBar">
            <Grid Background="Transparent">
                <Track x:Name="PART_Track" Orientation="Horizontal">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollPage}"
                                      Command="ScrollBar.PageLeftCommand"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollThumb}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollPage}"
                                      Command="ScrollBar.PageRightCommand"/>
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="11"/>
            <Setter Property="MinWidth" Value="11"/>
            <Setter Property="Template" Value="{StaticResource VerticalScrollBar}"/>
            <Style.Triggers>
                <!-- Orientation is set by the ScrollViewer after the style is
                     applied, so the horizontal case has to be a trigger rather
                     than a second style. -->
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="Height" Value="11"/>
                    <Setter Property="MinHeight" Value="11"/>
                    <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Collapsible category header in the channel list. -->
        <Style x:Key="GroupHeader" TargetType="ToggleButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="b" Background="{DynamicResource Header}" Padding="6,5"
                                BorderBrush="{DynamicResource Line}" BorderThickness="0,1,0,1">
                            <DockPanel>
                                <TextBlock x:Name="Arrow" DockPanel.Dock="Left"
                                           Text="&#xE70D;" FontFamily="Segoe MDL2 Assets" FontSize="8"
                                           Foreground="{DynamicResource Muted}"
                                           VerticalAlignment="Center" Margin="2,0,8,0"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="Arrow" Property="Text" Value="&#xE76C;"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource Hover}"/>
                                <Setter TargetName="Arrow" Property="Foreground" Value="{DynamicResource Text}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
