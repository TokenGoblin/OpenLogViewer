<Window x:Class="OpenLogViewer.App.FaultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OpenLogViewer.App"
        Title="Fault codes"
        Height="620" Width="720"
        MinHeight="420" MinWidth="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Bg}"
        ShowInTaskbar="False">

    <!--
        A frame around the panel and nothing else.

        The same panel is the calibration tab on an OBD2 connection, and two
        copies of a diagnostic view would drift — one of them would gain the fix
        and the other would keep quietly showing something else.
    -->
    <local:FaultsPanel x:Name="Panel"/>
</Window>
